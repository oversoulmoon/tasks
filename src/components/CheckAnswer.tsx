import { useState } from "react";
import { Form } from "react-bootstrap";

export function CheckAnswer({ expectedAnswer }: { expectedAnswer: string }) {
    const [answer, setAnswer] = useState<string>("");
    return <div></div>;
}
